model: Unet
encoder: resnet34

image_size: 256
batch_size: 4
epochs: 5

optimizer: Adam
learning_rate: 0.0001

loss:
  - BCEWithLogitsLoss
  - DiceLoss

pos_weight: 8.0

dataset:
  train_path: train/
  val_path: val/
  test_path: test/

seed: 42
